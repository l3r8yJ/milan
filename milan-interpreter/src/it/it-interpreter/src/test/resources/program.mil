BEGIN
    a1 := 1;
    WHILE a1 < 10 DO
        b1 := (a1 / 2) * 2;
        OUTPUT(b1);
        IF a1 == 9 THEN
            WHILE b1 <> 1 DO
                OUTPUT(b1);
                b1 := b1 - 1;
            ENDDO;
        ENDIF;
        a1 := a1 + 1;
    ENDDO;
    OUTPUT(a1);
    OUTPUT(b1);
    c101 := (a1 - 30) * (b1 + 2);
    OUTPUT(c101);
    OUTPUT(65433214);
END
