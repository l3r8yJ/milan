BEGIN
    a1 := 1;
    WHILE a1 < 10 DO
        b1 := a1 / 2;
        OUTPUT(a1);
        IF a1 == 9 THEN
            // спуск
            WHILE b1 <> 1 DO
                OUTPUT(b1);
                b1 := b1 - 1;
            ENDDO
        ENDIF
        a1++;
    ENDDO
END
